"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const build_1 = __importDefault(require("../commands/build"));
const utils_1 = require("../utils");
exports.default = async ({ target = process.env.FRONTITY_BUILD_TARGET || "both", development = !!process.env.FRONTITY_BUILD_DEVELOPMENT, publicPath = process.env.FRONTITY_BUILD_PUBLIC_PATH || "/static/", }) => {
    // Check `target` parameter.
    if (target && target !== "es5" && target !== "module" && target !== "both") {
        utils_1.errorLogger(new Error(`The target specified is invalid: "${target}". Use either "module", "es5" or "both".`));
    }
    // Execute `build` command.
    build_1.default({
        target: target,
        development,
        publicPath,
    });
};
